- insert:
    - id: dsh-remote
      name: '@dsh-remote/plugin'
      inject: [sessions, agents, approval, apiProxy, connection]
      config:
        enabled: true
        role: both
        serverUrl: https://dsh.r2049.cn
