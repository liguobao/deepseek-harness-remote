- insert:
    - id: dsh-remote
      name: '@dsh-remote/plugin'
      inject: [sessions, agents, approval, settings, apiProxy, connection]
      config:
        enabled: true
        role: host
        serverUrl: https://dsh.r2049.cn
